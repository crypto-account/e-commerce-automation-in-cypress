version: 2.1
orbs:
  cypress: cypress-io/cypress@6
workflows:
  build:
    jobs:
    - cypress/install
    - cypress/run:
          requires:
            - cypress/install
          browser: chrome
          record: false
          parallel: false
          group: "End-to-End Tests"
            
