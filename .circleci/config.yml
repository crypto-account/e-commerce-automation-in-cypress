version: 2.1
orbs:
  cypress: cypress-io/cypress@6
workflows:
  build:
    jobs:
      - cypress/run:
          browser: chrome
          record: false # Skoro nie używasz Cypress Cloud
          parallel: false
          group: "End-to-End Tests"
            
